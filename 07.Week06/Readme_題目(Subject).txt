HW7-1.請試寫一個函數void kitty(int)，讓主程式選單中選擇本題後，會先讓使用者在主程式中輸入一個介於3~9的數字n，如果超過此範圍會產生錯誤訊息，若符合3~9的數字，則主程式會呼叫kitty(n)，然後kitty()函數會自動產生n行的”Hello Kitty”字樣，執行完成後，使用者按任意健會返回主選單，讓使用者繼續選擇要執行的其它題目。

 

HW7-2.請試寫一個函數void star(int)，讓主程式選單中選擇本題後，會先讓使用者在主程式中輸入一個介於3~9的數字n，如果超過此範圍會產生錯誤訊息，若符合3~9的數字，則主程式會呼叫star(n)，然後star()函數會自動產生n層由英文字母所組成的三角形圖騰，每一行的英文字母需介於A~第n個字母中不斷循環. (以下僅為n=5英文字母A~E的狀況, 你需要同時處理n介於3~9的情形)，執行完成後，使用者按任意健會返回主選單，讓使用者繼續選擇要執行的其它題目。

             A

            ABC

           ABCDE

          ABCDEAB

         ABCDEABCD

 

HW7-3.請試寫一個函數void num(int)，讓主程式選單中選擇本題後，會先讓使用者在主程式中輸入一個介於10~100的數字n，如果超過此範圍會產生錯誤訊息，若符合10~100的數字，則主程式會呼叫num(n)，然後num()函數會列出此數之所有正因數。 【說明】此題目所說之因數均為正因數，不考慮負因數的情況。

[範例一]請輸入一個10~100的數字：100
100的所有因數為1 2 4 5 10 20 25 50 100

[範例二]請輸入一個10~100的數字：11
11的所有因數為1 11

 

HW7-4.請試寫一個函數int cub(int x)，讓主程式選單中選擇本題後，會先讓使用者在主程式中輸入一個整數數字x，則主程式會呼叫cub(x)，然後cub()函數會計算出x的3次方結果，並傳回到主程式由主程式顯示出X的3次方結果。

 

HW7-5.試寫一個函數double square(double x)，讓主程式選單中選擇本題後，會先讓使用者在主程式中輸入一個浮點數的數字x，則主程式會呼叫square (x)，然後square ()函數會計算出x的平方結果，並傳回到主程式由主程式顯示出X的平方結果。(此題要能算出包含小數的平方結果) 

 

HW7-6.試寫一個函數int mod(int x, int y)，讓主程式選單中選擇本題後，會先讓使用者在主程式中輸入二個整數數字x與y，則主程式會呼叫mod (x,y)，然後mod()函數會計算出x除以y的餘數結果，並傳回到主程式由主程式顯示出x/y的餘數。

 

HW7-7.試寫一個函數double f(double x)，讓主程式選單中選擇本題後，會先讓使用者在主程式中輸入一個浮點數x，則主程式會呼叫f(x)，然後f()函數會計算出f(x)=3x3+2x-1的結果，並傳回到主程式由主程式顯示出f(x)計算後的結果。

 